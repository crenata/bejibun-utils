/** Represents a single enum entry with a human-readable name and its underlying value. */
export type EnumItem = {
    /** Human-readable label for the enum entry. */
    name: string;

    /** Underlying value backing the enum entry. */
    value: any;
};
